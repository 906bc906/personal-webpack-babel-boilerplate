var root = document.querySelector("div#none");
root?.appendChild(document.createTextNode("Optional Chaining"));

//ES6 includes method
let arr = [1, 2, 3];
console.log(arr.includes(3), arr.includes(4));